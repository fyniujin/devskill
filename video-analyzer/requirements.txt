# video-analyzer Python 依赖
# 安装命令: pip install -r requirements.txt

# === 核心依赖 ===
openai-whisper>=20231117
numpy>=1.24.0
PyYAML>=6.0
Pillow>=10.0.0
opencv-python>=4.8.0

# === 可选依赖（说话人分离）===
# librosa>=0.10.0
# scikit-learn>=1.3.0

# === 可选依赖（OCR 功能）===
# paddleocr>=2.7.0
# paddlepaddle>=2.5.0

# === 可选依赖（实时资源监控）===
# psutil>=5.9.0

# === 可选依赖（高级可视化）===
# matplotlib>=3.7.0
