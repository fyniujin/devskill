"""国产大模型统一路由 — 脚本包。

纯标准库实现，无任何第三方依赖。对外只暴露一个统一 chat() 入口，
内部按任务自动或手动选择文心 / 通义 / 智谱 / DeepSeek / Kimi / 混元 / 豆包 等后端，
并自动统计每家 token 消耗与花费。

设计原则：
- 零依赖：仅用 Python 标准库（urllib / sqlite3 / json / hashlib …），安装即用，不拖累电脑。
- 密钥安全：所有厂商 Key 仅从环境变量读取，绝不写进技能包 / 不进 Git。
- 可观测：每次调用落本地 SQLite，跨模型统一计费（第 7 类：AI 成本 / 用量可观测）。
- 硬件自适应：自动探测 CPU / 内存，自动限制并发与批大小。
"""

__version__ = "1.0.0"
